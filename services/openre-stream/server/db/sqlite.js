'use strict';

const { SERVICE_NAME, SCHEMA_SQL, defaultSqlitePath, createSqliteStore } = require('../db.js');

module.exports = {
    SERVICE_NAME,
    SCHEMA_SQL,
    defaultSqlitePath,
    createSqliteStore,
};
